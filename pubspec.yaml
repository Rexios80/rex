name: rex
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  ansicolor: ^2.0.1
  args: ^2.3.2
  collection: ^1.17.0
  path: ^1.8.3
  pub_api_client: ^2.3.0

dev_dependencies:
  rexios_lints: ^6.0.1

executables:
  rex: rex
